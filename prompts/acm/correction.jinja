# ACM Record Correction

You are an expert at correcting Asbestos Containing Material (ACM) record field values. A previous extraction produced values that failed validation. Your task is to correct ONLY the specific fields listed below.

## Original Record

```json
{{ record_json }}
```

## Validation Errors

The following fields have invalid values:

{% for issue in validation_issues %}
- **{{ issue.field_name }}**: current value `{{ issue.current_value }}` is not valid
  - Valid values: {{ issue.valid_values | join(', ') }}
  - Issue: {{ issue.issue_type }}
{% endfor %}

## Correction Instructions

1. For each field listed above, determine the correct value from the valid values list
2. Consider the context of the full record when making corrections
3. If the original value is a synonym or close match, map it to the closest valid value
4. If you cannot determine a reasonable correction, use the most conservative valid value

**Common mappings:**
- "Bonded" → "Non-friable"
- "Medium" → "Moderate" (Salesforce uses "Moderate" not "Medium")
- "Detected" → "Positive"
- "Not Detected" / "NAD" → "Negative"
- "Negative - Treated as Positive" is a VALID SF value — do NOT simplify it to "Negative". These records are positive-managed and require Friability to be populated.
- "Positive - Non-friable" is a VALID SF value — do NOT simplify it to "Positive". It means the result is positive AND the material is non-friable.
- "Positive - Friable" is a VALID SF value — do NOT simplify it to "Positive". It means the result is positive AND the material is friable.
- If a record has result "Positive - Non-friable", the friable field must be "Non-friable"
- If a record has result "Positive - Friable", the friable field must be "Friable"
- For negative results: Condition and Disturbance Potential should be "N/A (negative)"

## Output Format

Return a JSON object with ONLY the corrected fields. Do not include fields that were not flagged as errors.

Example: `{"friable": "Non-friable", "disturbance_potential": "Moderate"}`
