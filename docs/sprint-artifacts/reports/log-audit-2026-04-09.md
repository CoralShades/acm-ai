# Service Log Audit — 2026-04-09

**Audited at:** ~21:35 AEST (11:35 UTC)  
**Audited by:** log-monitor agent (acm-full-audit team)

---

## Service Health Summary

| Service | Port | Container | Status | Notes |
|---------|------|-----------|--------|-------|
| SurrealDB | 8000 | `acm-ai-db` | **HEALTHY** | Up 8 min, version 2.2.1 |
| FastAPI Backend | 5055 | (native process) | **HEALTHY** | `/health` → `{"status":"healthy"}` |
| Frontend (Next.js) | 8502 | (native process) | **HEALTHY** | HTML served, Next.js 15.5.12 |
| Ollama | 11434 | `acm-ai-ollama` | **UNHEALTHY (Docker)** | API responds, GPU detected; healthcheck failing |
| Langfuse | 3000 | `acm-ai-langfuse` | **HEALTHY** | UI serving, worker clean |
| Langfuse Worker | 3030 | `acm-ai-langfuse-worker` | **HEALTHY** | Scheduled jobs running |
| Langfuse ClickHouse | 8123 | `acm-ai-langfuse-clickhouse` | **HEALTHY** | |
| Langfuse Postgres | 5433 | `acm-ai-langfuse-postgres` | **HEALTHY** | |
| Langfuse MinIO | 9090 | `acm-ai-langfuse-minio` | **HEALTHY** | |
| Langfuse Redis | 6379 | `acm-ai-langfuse-redis` | **HEALTHY** | |

---

## SurrealDB Logs

**Container:** `acm-ai-db` — Up 8 minutes (started 2026-04-09T11:33:25Z), healthy  
**Version:** 2.2.1 for linux/x86_64  
**Storage:** SurrealKV at `mydata/open_notebook.db` (versions disabled)

### Warnings

```
WARN Credentials were provided, but existing root users were found.
     The root user 'root' will not be created
WARN Consider removing the --user and --pass arguments from the server start command
```

These warnings fire on every restart because `--user root --pass root` args are passed to a DB that already has root credentials stored. This is cosmetic but noisy.

### Errors
None.

---

## FastAPI Backend Logs

**Process:** Running on port 5055 (native, `uv run run_api.py`)  
**Health endpoint:** `GET /` → `{"message":"ACM-AI API is running"}` ✓  
**Health endpoint:** `GET /health` → `{"status":"healthy"}` ✓

### Live API Tests

| Endpoint | Result | Status |
|----------|--------|--------|
| `GET /api/sources?limit=3` | 2 sources returned (AlexanderHospital, Broadmeadows) | 200 OK |
| `GET /api/acm/buildings?source_id=source:qnt6w2t1h251x0y0uxpw` | 7 buildings returned | 200 OK |
| `GET /api/acm/validation-summary?source_id=source:qnt6w2t1h251x0y0uxpw` | Returns `{"buildings":[]}` (empty) | 200 OK but suspicious |
| `GET /api/models` | 27 models returned | 200 OK |

**Note on validation-summary:** The endpoint returned `{"buildings":[]}` rather than validation statistics. This is unexpected — the endpoint should return validation counts (accepted/rejected/with_issues). This warrants investigation.

### Historical Errors (from `api-error.log`, dated 2026-03-22)

These errors are from test runs and use mock paths (`/tmp/fake.pdf`):

```
ERROR api.routers.acm:list_raw_extractions | Error listing raw extractions for source source:abc: DB connection failed
ERROR open_notebook.extractors.row_extractor | Row 1/1: extraction failed: No JSON object found in response text
ERROR scripts.v3_data_migration:verify_schema | building_record table does not exist
ERROR pipeline_logger | EXTRACTION FAILED | Model timeout
ERROR pipeline_logger | [DOCLING] FAILED | Docling import failed
ERROR open_notebook.database.repository:repo_query | Found NONE for field `run_id`, record `extraction_progress:command_acm_extract_123`
```

**Assessment:** All from test suite execution on 2026-03-22. Not production errors.

### Historical Errors (from `api-restart.log`, around 2026-03-16)

```
ERROR open_notebook.database.repository:repo_query | 
      Versioned error: A deserialization error occured: Invalid revision `0` for type `Value`
ERROR api.routers.acm:get_validation_summary | 
      Error fetching validation summary for source:iyklekqc55w11kiovdwu: Versioned error ...
→ HTTP 500 on GET /api/acm/validation-summary
```

**Assessment:** This is a real production error from 2026-03-16. The `validation-summary` endpoint was returning 500 due to a SurrealDB deserialization error with `revision=0`. It may have been patched since — current test returns 200 with `{"buildings":[]}` (no 500), but the response is empty/wrong, suggesting the underlying query may still have issues.

---

## Frontend Logs

**Process:** Running on port 8502 (Windows nvm4w / Node.js)  
**Version:** Next.js 15.5.12  
**Log file:** `logs/frontend.log` (UTF-16LE from Windows PowerShell, appears garbled in WSL)

### Warnings (decoded from UTF-16LE)

```
WARNING: The "experimental.esmExternals" option has been modified. 
         experimental.esmExternals is not recommended to be modified as it may 
         disrupt module resolution. It should be removed from your next.config.ts.

WARNING: Reverting webpack devtool to 'false'. Changing the webpack devtool in 
         development mode will cause severe performance regressions.
         Read more: https://nextjs.org/docs/messages/improper-devtool
```

### Info
- Ready in 6.1s
- Proxying `/api/*` to `http://localhost:5055/api/*`
- Pages compiling without errors
- HTTP 200 responses for `/` and `/config`

### Errors
None observed in current session.

---

## Ollama Logs

**Container:** `acm-ai-ollama` — Up 2 weeks (started ~2026-03-23), **Docker health: UNHEALTHY**  
**GPU:** NVIDIA GeForce RTX 4090, 24.0 GiB total, 22.4 GiB available  
**Version:** 0.11.2

### Loaded Models
- `mxbai-embed-large:latest` (334M embedding, BERT)
- `llama3.2:latest` (3.2B, Q4_K_M)
- `phi4:14b` (14.7B, Q4_K_M)
- `llama3.1:8b` (8.0B, Q4_K_M)

### API Health
`GET /api/tags` → 200 OK, all 4 models listed ✓

### Docker Healthcheck Issue
Container is marked `(unhealthy)` despite API responding correctly. The Docker healthcheck likely probes in a way that times out or fails to match the expected output. The Ollama service itself is fully functional.

**Last log entry:** 2026-03-24 09:01:00 — no errors, only startup sequence.

### Errors
None in logs. Docker `(unhealthy)` status is a false alarm — healthcheck configuration mismatch.

---

## Langfuse Logs

**Container:** `acm-ai-langfuse` — Up 2 weeks, healthy  
**UI:** Serving on port 3000 ✓

### Langfuse Worker Logs (recent)

```
2026-04-09T08:30:00Z info [POSTHOG] No PostHog integrations ready for sync
2026-04-09T09:20:00Z info Executing Blob Storage Integration Job
2026-04-09T10:30:00Z info [MIXPANEL] No Mixpanel integrations ready for sync
2026-04-09T11:20:00Z info Executing Blob Storage Integration Job
```

All scheduled jobs running cleanly. No errors in worker.

### Langfuse API Errors (container logs)

```
ZodError: [{"code":"invalid_value","values":["ASC","DESC"],"path":["orderBy","order"],
            "message":"Invalid option: expected one of \"ASC\"|\"DESC\""}]
at Object.GET /api/public/traces

API response for /api/public/traces exceeds 4MB. 
API Routes are meant to respond quickly.
```

**Assessment:** Internal Langfuse error — the `/api/public/traces` endpoint is receiving an invalid sort order parameter (likely from a dashboard query) and the traces response exceeds 4MB. This is a Langfuse internals issue, not an ACM application error. Low priority.

---

## Error Summary

| Severity | Service | Error | Status |
|----------|---------|-------|--------|
| LOW | SurrealDB | Credential warning on startup (cosmetic) | Known, harmless |
| LOW | Ollama | Docker healthcheck `(unhealthy)` despite API working | Healthcheck misconfiguration |
| LOW | Frontend | `experimental.esmExternals` warning in next.config.ts | Next.js config debt |
| LOW | Frontend | Webpack devtool warning | Next.js config debt |
| LOW | Langfuse | ZodError on `/api/public/traces` (sort order) | Langfuse internal issue |
| LOW | Langfuse | Traces API response >4MB | Langfuse internal issue |
| MEDIUM | FastAPI | `validation-summary` returns `{"buildings":[]}` (empty, not validation data) | Regression or data issue |
| HISTORICAL | FastAPI | `validation-summary` was returning HTTP 500 (2026-03-16) | May be fixed (no 500 now) |
| HISTORICAL | FastAPI/Worker | Test-run errors (2026-03-22) in `api-error.log` | Test artifacts, not production |

---

## Recommendations

### Priority 1 — Investigate
1. **validation-summary endpoint returning empty** (`{"buildings":[]}`): The endpoint at `GET /api/acm/validation-summary?source_id=...` returns `{"buildings":[]}` for source `qnt6w2t1h251x0y0uxpw` which has 120 records and 7 buildings. This should return validation statistics. The old HTTP 500 is gone but the data is wrong. Check `api/routers/acm.py` around line 2732.

### Priority 2 — Fix Config Debt
2. **Remove `experimental.esmExternals: "loose"`** from `frontend/next.config.ts` — Next.js warns this disrupts module resolution.
3. **Remove `--user`/`--pass` args** from SurrealDB Docker start command to eliminate startup warning (or suppress it if intentional for reset capability).

### Priority 3 — Investigate Healthcheck
4. **Fix Ollama Docker healthcheck** — the container has been `(unhealthy)` for 2 weeks but works fine. The healthcheck command in `docker-compose.yml` likely has incorrect expected output or timeout. This causes false alerts.

### Priority 4 — Monitor
5. **Langfuse traces API size** — if the trace database grows, the 4MB limit will cause more failures. Consider adding pagination or filters to dashboard queries.
6. **SurrealDB deserialization errors** — the `Invalid revision '0'` error from 2026-03-16 appears resolved (no 500 now), but keep an eye on validation-summary responses.

---

*Report generated by log-monitor agent — acm-full-audit team*
